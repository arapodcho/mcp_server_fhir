# Core runtime
requests>=2.31.0
pandas
mcp>=1.0.0
httpx>=0.25.0
python-dotenv>=1.0.0

# Agent runtime (LangGraph + Gemini)
# NOTE: This repo uses LangChain 0.3.x (langchain-core < 1.0). Keep LangGraph/GenAI
# integrations in compatible major versions.
langgraph>=0.3.0,<0.4
langchain-google-genai>=2.1.0,<3
